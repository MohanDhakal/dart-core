import 'package:new_project/level1/functions.dart';
import 'package:new_project/level1/high_func.dart';

void main() {
  // multiplyResult(3, 17, getFloor);
  // print(myfn(2));
  // closure1Check();
  cookDinner();

  // print(mytask(3));
  // enableFlags(bold: false,hidden: true);
  // defineUser('mohan', 'kathmandu',);

  // checkControlFlow();
  // checkConst();
  // opDemo();
  // checkFinal();
//   void printElement(int element) {
//     print(element);
//   }

//   var list = [1, 2, 3];

// // Pass printElement as a parameter.
//   list.forEach(printElement);
  // Future<int> getSelectedInteger() async {
  //   await Future.delayed(Duration(seconds: 2));
  //   return 3;
  // }

  // void printSelectedNumber() async {
  //   var a = await getSelectedInteger();
  //   print(a);
  // }

  // printSelectedNumber();
}
